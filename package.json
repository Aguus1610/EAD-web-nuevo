{
  "name": "ead-oleohidraulica",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "lint": "astro check",
    "format:check": "astro check",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "instagram:import": "node --env-file-if-exists=.env.local scripts/import-instagram.ts",
    "check:build": "node scripts/check-build.mjs",
    "validate:launch": "node scripts/validate-launch.mjs",
    "quality": "npm run typecheck && npm run lint && npm test && npm run build && npm run check:build && npm run validate:launch && npm run test:e2e"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.0.0",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
